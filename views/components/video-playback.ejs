<!-- views/components/video-playback.ejs -->
<section class="video-section">
    <div class="split-layout">

        <!-- Sisi Kanan: Konten Video -->
        <div class="split-right">
            <div class="split-bg-image" style="background-image: url('/images/SLM09319.JPG');"></div>
            <div class="split-overlay-dark"></div>

            <div class="video-content-wrapper">
                <div class="video-header">
                    <p class="video-subtitle">UNVEILING</p>
                    <h2 class="video-title">OUR PREWEDDING STORY</h2>
                </div>

                <div class="video-player-container" id="videoContainer">
                    <video id="prewedVideo" class="content-video" poster="/images/SLM08669.JPG">
                        <source src="/videos/full clip Prewed Saharsa 11 April .mp4" type="video/mp4">
                    </video>

                    <!-- Overlay Kontrol: Muncul saat Hover atau saat Pause -->
                    <div class="video-controls-overlay" id="videoControls">
                        <button class="play-pause-btn" onclick="toggleVideo()" id="mainBtn">
                            <!-- Ikon Play -->
                            <svg id="playIcon" viewBox="0 0 24 24" fill="currentColor" class="icon-svg">
                                <path d="M8 5v14l11-7z" />
                            </svg>
                            <!-- Ikon Pause (Hidden by default) -->
                            <svg id="pauseIcon" viewBox="0 0 24 24" fill="currentColor" class="icon-svg hidden">
                                <path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z" />
                            </svg>
                        </button>
                    </div>
                </div>

                <div class="video-quote-box">
                    <p class="handwriting-text">
                        Every love story is beautiful, but ours is my favorite.
                        Through the highs and lows, our love grows stronger.
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    const video = document.getElementById('prewedVideo');
    const controls = document.getElementById('videoControls');
    const playIcon = document.getElementById('playIcon');
    const pauseIcon = document.getElementById('pauseIcon');

    function toggleVideo() {
        if (video.paused) {
            video.play();
            updateUI(true);
        } else {
            video.pause();
            updateUI(false);
        }
    }

    function updateUI(isPlaying) {
        if (isPlaying) {
            playIcon.classList.add('hidden');
            pauseIcon.classList.remove('hidden');
            // Tambahkan class agar overlay bisa di-handle CSS (hidden saat play)
            controls.classList.add('is-playing');
        } else {
            playIcon.classList.remove('hidden');
            pauseIcon.classList.add('hidden');
            // Pastikan overlay muncul kembali saat pause
            controls.classList.remove('is-playing');
        }
    }

    video.onended = () => updateUI(false);
</script>